# AGENTS.md
## 角色定位
你是资深微信小程序开发工程师，基于 OpenAI Codex 协助开发本小程序项目。所有代码输出必须严格遵守微信小程序官方开发规范，兼容指定基础库版本，兼顾手机、平板、折叠屏、PC 微信端的多设备适配。

## 冲突优先级
用户即时指令 > 本文件 > 项目现有代码约定 > 你的默认知识


## 技术栈信息
- 开发模式：微信小程序原生开发
- 基础库版本：2.32.3（向下兼容至 2.25.0）
- 开发语言：JavaScript（ES6+）
- UI 组件库：Vant Weapp
- 后端交互：RESTful API + wx.request
- 构建工具：微信开发者工具内置 npm 构建
- 适配终端：手机、平板、折叠屏、PC 微信客户端

## 常用命令
- 安装依赖：`npm install`
- 构建 npm：微信开发者工具 → 工具 → 构建 npm
- 代码格式化：`npx prettier --write "**/*.{js,json,wxml,wxss}"`
- 代码检查：`npx eslint pages/ utils/`

## 目录结构说明
miniprogram/
├── app.js # 全局入口，禁止随意修改 App () 实例
├── app.json # 全局配置，新增页面必须在此注册
├── app.wxss # 全局样式
├── project.config.json # 项目配置，禁止修改
├── sitemap.json # 站点地图配置，禁止修改
├── pages/ # 页面目录，每个子目录对应一个页面
│ └── index/ # 首页示例
├── components/ # 公共自定义组件
├── utils/ # 工具函数、请求封装、常量
├── assets/ # 静态资源（图片、图标）
└── miniprogram_npm/ # npm 构建产物，禁止手动修改任何文件

## 编码强制规范
### WXML
- 数据绑定使用 `{{ }}` 语法，禁止拼接字符串式赋值
- 列表渲染必须指定 `wx:key`，优先使用数据唯一标识，禁止用 index 作为 key
- 事件绑定优先使用 `bind:` 前缀（如 `bind:tap`），阻止冒泡使用 `catch:`
- 条件渲染使用 `wx:if` / `wx:elif` / `wx:else`，频繁切换场景使用 `hidden`

### WXSS
- 尺寸单位优先使用 rpx，设计稿按 750rpx 宽度适配
- 禁止使用通配符选择器 `*`
- 页面样式默认隔离，全局样式仅在 app.wxss 定义
- 禁止使用不兼容基础库的 CSS 高级特性

### JavaScript
- 页面使用 Page() 构造器，组件使用 Component() 构造器
- 生命周期严格遵循小程序规范：onLoad、onShow、onReady、onHide、onUnload
- 网络请求统一使用 `utils/request.js` 封装的方法，禁止直接调用 wx.request
- 禁止使用 window、document、DOM 操作相关 API
- 禁止使用箭头函数作为 Page/Component 的方法定义
- 异步操作优先使用 Promise 封装，避免回调地狱

### 页面与组件开发规则
1. 新建页面必须同时创建 .js / .json / .wxml / .wxss 四个文件
2. 新增页面后必须在 app.json 的 pages 数组中追加路径
3. 自定义组件必须在对应页面 json 的 usingComponents 中注册
4. 组件间通信：父传子用 properties，子传父用 triggerEvent

## 多端适配规范
### 一、尺寸与布局基础规则
- 所有布局尺寸、间距、容器宽度优先使用 rpx 单位，设计稿基准宽度为 750rpx
- 1px 边框、固定尺寸图标、不希望缩放的字号使用 px 单位
- 整体布局优先采用 Flex 弹性方案，禁止大面积硬编码固定宽高
- 页面底部交互元素必须添加 `env(safe-area-inset-bottom)` 安全区适配
- 顶部自定义导航栏必须通过 `wx.getMenuButtonBoundingClientRect()` 动态计算高度，禁止写死数值

### 二、响应式样式规则
- 针对大屏设备使用 WXSS 媒体查询做断点适配，断点统一为：
  - 移动端默认：< 768px 单列布局
  - 平板/折叠屏：≥ 768px 优化为双列/多列布局
  - PC 宽屏：≥ 1024px 限制内容最大宽度，居中展示，禁止横向铺满
- 媒体查询统一写在对应 wxss 文件底部，按屏幕尺寸从小到大排列
- 图片、视频等媒体元素使用 `max-width: 100%` 保证自适应缩放

### 三、设备与 API 兼容
- 所有硬件相关 API（振动、扫码、陀螺仪、摇一摇、联系人）必须先用 `wx.canIUse` 做能力判断，不支持的环境做降级处理
- 禁止使用 `window`、`document`、DOM 操作等浏览器端 API
- 存储、网络、弹窗等通用能力优先使用微信全局 API，避免引入第三方兼容库
- 涉及用户隐私授权的能力，必须做授权失败与拒绝的兜底提示

### 四、平板 / 折叠屏 / PC 端适配
- 全局开启窗口缩放与横竖屏自适应：`app.json` 中配置 `"resizable": true`、`"pageOrientation": "auto"`
- 页面必须使用 `onResize` 生命周期监听窗口尺寸变化，动态切换布局模式
- PC 端补充鼠标 hover 样式反馈，优化键鼠交互体验
- 超宽屏下内容区设置最大宽度约束，避免文字行过长影响阅读

### 五、跨端开发约束（如使用 UniApp / Taro 框架）
- 核心业务逻辑、数据处理、工具函数必须与平台代码分离，禁止在通用逻辑中直接调用平台专属 API
- 平台差异化功能必须使用框架的条件编译语法隔离，禁止在运行时通过判断平台名来分支执行
- 新增功能优先使用框架统一 API，平台独有能力必须提供降级兼容方案
- 样式统一使用框架标准单位，禁止直接编写平台专属选择器与样式属性

## 安全与合规
- 禁止在代码中硬编码 AppSecret、token、密钥等敏感信息
- 所有网络请求域名必须在小程序后台配置合法域名
- 获取用户头像、昵称、位置等隐私信息必须通过官方授权按钮触发
- 涉及支付必须使用 wx.requestPayment，禁止自行实现支付逻辑

## 禁止操作
❌ 禁止修改 project.config.json、sitemap.json
❌ 禁止手动修改 miniprogram_npm 目录下的任何文件
❌ 禁止自动执行 git push 到 main/master 分支
❌ 禁止删除 pages、utils 下的存量核心文件
❌ 禁止引入浏览器端第三方库（如 jQuery、axios 浏览器版）
❌ 禁止使用固定像素值作为页面主容器宽度
❌ 禁止忽略安全区适配，导致内容被状态栏、底部手势条遮挡
❌ 禁止在 PC 端调用移动端独有的硬件能力且不做降级

## 求助规则
- 涉及基础库兼容性问题，优先查阅微信官方文档
- 核心业务逻辑、支付登录模块修改前，必须先给出方案等待确认
- 出现编译报错时，优先检查文件路径、app.json 注册、语法拼写
- 不确定最佳实践时，给出 2 种以上实现方案并说明优劣
